{{#if view.topic}}
  <h3>{{i18n 'topic_stats.title' }}</h3>
  <dl>
    <dt>{{i18n 'topic_stats.created'}}</dt>
    <dd>{{bound-date view.topic.created_at}}</dd>
    <dt>{{i18n 'topic_stats.last_answer'}}</dt>
    <dd>{{bound-date view.topic.last_posted_at}}</dd>
    <dt>{{i18n 'topic_stats.views'}}</dt>
    <dd>{{digitGrouping view.topic.views}}</dd>
    {{#if view.topic.total_reply_count}}
      <dt>{{i18n 'categories.answers'}}</dt>
      <dd>{{digitGrouping view.topic.total_reply_count}}</dd>
    {{else}}
      <dt>{{i18n 'topic_stats.posts'}}</dt>
      <dd>{{digitGrouping view.topic.posts_count}}</dd>
    {{/if}}
  </dl>
  <div class="participants">
    <h4>{{i18n 'topic_stats.participants'}}</h4>
    <div>
      {{#if view.participantsCollapsed}}
        {{#each participant in view.topic.details.fewParticipants}}
          {{topic-participant participant=participant}}
        {{/each}}
      {{else}}
        {{#each participant in details.participants}}
          {{topic-participant participant=participant}}
        {{/each}}
      {{/if}}
    </div>
  </div>
  {{#if view.category.moderators}}
    <div class="moderators">
      <span>{{i18n 'category.mod_description'}}
        {{moderatorList view.category.moderators}}
      </span>
    </div>
  {{/if}}
{{/if}}

